import AddServiceContent from './AddServiceContent';

export const dynamic = 'force-dynamic';

export default function AddServicePage({ searchParams }) {
  return <AddServiceContent searchParams={searchParams} />;
}
